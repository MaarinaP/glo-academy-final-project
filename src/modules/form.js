"use strict";

const form = () => {
    const body = document.querySelector("body");
    const inputName = body.querySelectorAll("input[name=fio]");
    const inputNumber = body.querySelectorAll("input[name=phone]");
};

export default form;
